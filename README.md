# zig-sqlite

## Build options

```zig
struct {
    enable_column_metadata: bool = false,
    enable_dbstat_vtab:     bool = false,
    enable_fts3:            bool = false,
    enable_fts4:            bool = false,
    enable_fts5:            bool = false,
    enable_geopoly:         bool = false,
    enable_icu:             bool = false,
    enable_math_functions:  bool = false,
    enable_rbu:             bool = false,
    enable_rtree:           bool = false,
    enable_stat4:           bool = false,
    omit_decltype:          bool = false,
    omit_json:              bool = false,
    use_uri:                bool = false,
}
```
